{
  "name": "medicine-inventory-tracker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "deploy": "wrangler deploy",
    "cf:migrate": "wrangler d1 migrations apply medicine-inventory --remote"
  },
  "devDependencies": {
    "wrangler": "^4.14.0"
  }
}
