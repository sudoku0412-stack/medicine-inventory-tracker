# Copy to deploy/cloudflared.yml (gitignored) or ~/.cloudflared/config.yml
# Replace YOUR_TUNNEL_ID and the credentials path (Kaushik Mac path shown).

tunnel: YOUR_TUNNEL_ID
credentials-file: /Users/kaushiksudesna/.cloudflared/YOUR_TUNNEL_ID.json

ingress:
  - hostname: medicineinventory.craftloop.ca
    service: http://127.0.0.1:3000
  - service: http_status:404
